<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="ArtApp.Views.DetailsTodoItemView">

  <ScrollView>
    <StackLayout>
      <Label Text="Todo Item" Font="Bold" VerticalOptions="Center" HorizontalOptions="Center"/>
      <StackLayout Orientation="Horizontal">
        <Label Text="Name" HorizontalOptions="Start" VerticalOptions="Center"/>
        <Label Text="{Binding Name}" HorizontalOptions="FillAndExpand" VerticalOptions="Center"/>
      </StackLayout>
      <StackLayout Orientation="Horizontal">
        <Label Text="Notes" HorizontalOptions="Start" VerticalOptions="Center"/>
        <Label Text="{Binding Notes}" HorizontalOptions="FillAndExpand" VerticalOptions="Center"/>
      </StackLayout>
      <StackLayout Orientation="Horizontal">
        <Label Text="Done" HorizontalOptions="Start" VerticalOptions="Center"/>
        <Switch IsToggled="{Binding Done}" HorizontalOptions="FillAndExpand" VerticalOptions="Center"/>
      </StackLayout>
      <Button Text="Actions" Command="{Binding DisplayTodoItemActionSheetCommand}"></Button>
    </StackLayout>
  </ScrollView>

</ContentPage>