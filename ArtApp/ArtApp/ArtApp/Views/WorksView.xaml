<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:ArtApp.ViewModels;assembly=ArtApp"
             x:Class="ArtApp.Views.WorksView" Padding="10">

  <ContentPage.BindingContext>
    <viewModels:WorksViewModel/>
  </ContentPage.BindingContext>

  <ContentPage.ToolbarItems>
    <ToolbarItem Text="Add" Command="{Binding CreateWorkCommand}"></ToolbarItem>
  </ContentPage.ToolbarItems>



  <ContentPage.Content>
    <StackLayout>
      <Label Text="WORKS VIEW"></Label>
      <SearchBar Text="{Binding SearchText, Mode=TwoWay}" Placeholder="Search" SearchCommand="{Binding SearchWorkCommand}"/>

      <ListView ItemsSource="{Binding Works}" HasUnevenRows="True" SeparatorVisibility="Default">
        <ListView.ItemTemplate>
          <DataTemplate>
            <ViewCell>
              <ViewCell.ContextActions>
                <MenuItem Text="Add" Command="{Binding CreateWorkCommand}"></MenuItem>
              </ViewCell.ContextActions>
              <StackLayout Orientation="Horizontal">
                <Label Text="{Binding Title}"/>
                <Label Text="{Binding Description}" />
              </StackLayout>
            </ViewCell>
          </DataTemplate>
        </ListView.ItemTemplate>
      </ListView>
    </StackLayout>




  </ContentPage.Content>


</ContentPage>