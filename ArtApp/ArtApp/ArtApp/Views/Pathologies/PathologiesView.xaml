<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="ArtApp.Views.PathologiesView">

  <ContentPage.ToolbarItems>
    <ToolbarItem Text="Add" Command="{Binding CreatePathologyCommand}"/>
  </ContentPage.ToolbarItems>

  <ContentPage.Content>
    <StackLayout>
      <Label Text="Pathologies" HorizontalOptions="Center" FontSize="20"/>
      <SearchBar Text="{Binding SearchText, Mode=TwoWay}" Placeholder="Search By Name" TextChanged="SearchBar_OnTextChanged"/>
      <ListView ItemsSource="{Binding PathologiesSearch}" HasUnevenRows="True"
                SeparatorVisibility="Default" SelectedItem="{Binding PathologySelected}"
                IsPullToRefreshEnabled="True" RefreshCommand="{Binding ResfreshPathologyListCommand}"
                IsRefreshing="{Binding isBusy, Mode=OneWay}">
        <ListView.ItemTemplate>
          <DataTemplate>
           <TextCell Text="{Binding Name}"/>
          </DataTemplate>
        </ListView.ItemTemplate>
      </ListView>
    </StackLayout>
  </ContentPage.Content>
</ContentPage>

