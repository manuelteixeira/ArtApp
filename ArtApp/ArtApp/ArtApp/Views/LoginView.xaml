<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:views="clr-namespace:ArtApp.Views;assembly=ArtApp"
             xmlns:viewModels="clr-namespace:ArtApp.ViewModels;assembly=ArtApp"
             x:Class="ArtApp.Views.LoginView">
  
<ContentPage.BindingContext>
  <viewModels:LoginViewModel/>
</ContentPage.BindingContext>  
  <ContentPage.Content>
    <StackLayout Padding="20">
      <Entry Text="{Binding Email}" Keyboard="Email" Placeholder="email@server.com"/>
      <Entry Text="{Binding Password}" IsPassword="True" Placeholder="Password"/>
      <Button Text="Login" Command="{Binding LoginCommand}"/>
      <Button Text="Register" Command="{Binding RegisterCommand}"/>
    </StackLayout>
  </ContentPage.Content>
</ContentPage>